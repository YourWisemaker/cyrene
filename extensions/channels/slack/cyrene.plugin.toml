name = "slack"
version = "0.1.0"
description = "Slack bot channel"
capabilities = ["channel"]
host_compat = ">=0.1.0"

[permissions]
network = true
secrets = ["SLACK_BOT_TOKEN", "SLACK_APP_TOKEN", "SLACK_SIGNING_SECRET"]
