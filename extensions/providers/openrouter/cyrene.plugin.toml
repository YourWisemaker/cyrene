name = "openrouter"
version = "0.1.0"
description = "OpenRouter model provider (300+ models behind one key)"
capabilities = ["model_provider"]
host_compat = ">=0.1.0"

[permissions]
network = true
secrets = ["OPENROUTER_API_KEY"]
