# Shared rustfmt configuration. Only stable-channel options are used here so
# `cargo fmt --check` succeeds on the pinned stable toolchain in CI.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
