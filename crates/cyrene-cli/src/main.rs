//! `cyrene` — the command-line entry point for the Cyrene agent.
//!
//! Placeholder scaffold (task 1). The full `clap` CLI (`agent`, `gateway`,
//! `dashboard`, `onboard`, `doctor`, `model`, `skills`, `hub`), the onboarding
//! wizard, and the startup banner are implemented in task 23. For now `main`
//! prints a placeholder so the workspace produces a runnable binary.

fn main() {
    println!(
        "cyrene {} — scaffold; CLI not yet implemented",
        env!("CARGO_PKG_VERSION")
    );
}
