name = "web-scraper"
version = "0.1.0"
description = "MCP server for advanced web scraping with JavaScript rendering"
capabilities = ["tool"]
host_compat = ">=0.1.0"

[permissions]
network = true

[tools]
provides = ["web.scrape", "web.screenshot", "web.extract"]
